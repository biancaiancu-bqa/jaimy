///<reference types="cypress"/>

//TO BE CONTINUED WHEN WORKING ON HO PAGE
describe('UI Main Page', () => {
    beforeEach(()=>{
        // cy.server();    
        // cy.ntlmReset();
    })
    it('Log in', () => {
        // cy.ntlm(["jaimystaging.be"], "admin", "Jaimy@20w19");

        // cy.visit('https://jaimystaging.be/en/?').wait(3000)
        // cy.contains()
       
    })


})